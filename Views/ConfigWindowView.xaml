<Window x:Class="SupabaseUserManager.Views.ConfigWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SupabaseUserManager.Views"
        mc:Ignorable="d"
        Title="Preferences" Height="250" Width="300">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Supabase URL:" Margin="0,0,0,5"/>
            <TextBox Margin="0,0,0,10" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Supabase Key:" Margin="0,0,0,5"/>
            <TextBox Margin="0,0,0,10" Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Save" Width="80" Height="30" HorizontalAlignment="Center"
                    Command="{Binding SaveConfigCommand}"/>
        </StackPanel>
    </Grid>
</Window>
