<UserControl x:Class="SupabaseUserManager.Views.UserListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SupabaseUserManager.Views"
             mc:Ignorable="d" d:Background="White"
             d:DesignHeight="250" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Controls -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10">
                <Button Content="Add User" Width="100" Margin="5,0,5,0"
                        Command="{Binding LaunchAddUserCommand}"/>
                <Button Content="Delete User" Width="100" Margin="5,0,5,0"
                        IsEnabled="{Binding IsSelected}"
                        Command="{Binding DeleteUserCommand}"/>
                <Button Content="Reload" Width="100" Margin="5,0,5,0"
                        Command="{Binding LoadUsersCommand}"/>
                <TextBlock Text="{Binding StatusMessage}" Margin="20,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" Grid.ColumnSpan="2">
            <!-- DataGrid to display users -->
            <DataGrid Grid.Column="1"
                      ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"/>
        </Grid>
    </Grid>
</UserControl>
